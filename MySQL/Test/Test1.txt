1. select ename, salary from employees order by salary desc;

2. select * from employees order by desc limit 5;

3. select * from employees limit 5 offset 3;

4. select dept_id,count(*) from employees group by dept_id;

5. select dept_id, avg(salary) from employee group by dept_id having avg(salary)>50000
  
6. select * from Products where product_name like "%Laptop%";

7. select substring(student_name,1,3) from Students;

8. select * from Students where student_name like "A%";

9. select month(order_date), count(*) from Products group by month(order_date) having count(*)>100;

10. select category, 
	sum(sales_amount) as total_sales, 
	avg(sales_amount) as average_sales from Sales 
	group by category 
	having sum(sales_amount)>100000; 

11. select * from employees where salary between 30000 and 60000;

12. select * from employees where department not in('HR','Admin');

13. select * from students where email_id is null;

14. select * from employees where dept_id in (1,3);

15. select * from employees where age>25 and salary<50000;

16. select dept_id,max(salary) as maximum_salary from employees group by dept_id;

17. select city,count(*) from Students group by city;

18. select min(salary) as Minimum_salary, max(salary) as Maximum_salary, avg(salary) as Average_salary from employees;

19. select dept_id,count(*) from employees group by dept_id having count(*) >5;

20. select dept_id, sum(salary) from employees group by dept_id order by sum(salary) desc;

21. update employee set emp_name = upper(emp_name);

22. select e_name,length(e_name) from employees;

23. select substring(student_name,-2) from Students;
 OR
    select right(student_name,2) from students;
24. select * from employee order by salary asc limit 3;

25. select * from Products order by price asc, product_name desc;

26. c - having

27. b - count()

28. b - is null

29. b - order by

30. b - check a range of values

31. c - avg()

32. b - group by

33. c - limit

34. c - like 

35. c - total number of rows
